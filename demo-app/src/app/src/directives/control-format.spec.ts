import { ControlFormat } from './control-format';

describe('ControlFormat', () => {
  it('should create an instance', () => {
    const directive = new ControlFormat();
    expect(directive).toBeTruthy();
  });
});
